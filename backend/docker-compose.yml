services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: clearfile
      POSTGRES_PASSWORD: clearfile
      POSTGRES_DB: clearfile
    ports:
      - "15432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: